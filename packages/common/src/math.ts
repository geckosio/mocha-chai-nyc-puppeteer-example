const sum = (a, b) => {
  return a + b
}

const multiply = (a, b) => {
  return a * b
}

const subtract = (a, b) => {
  return a - b
}

const divide = (a, b) => {
  return a / b
}

const pi = () => {
  return Math.PI
}

export default { sum, multiply, subtract, divide, pi }
