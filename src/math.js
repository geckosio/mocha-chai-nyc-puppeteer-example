const sum = (a, b) => {
  return a + b
}

const multiply = (a, b) => {
  return a * b
}

const subtract = (a, b) => {
  return a - b
}

const divide = (a, b) => {
  return a / b
}

const pi = () => {
  return Math.PI
}

module.exports = { sum, multiply, subtract, divide, pi }
